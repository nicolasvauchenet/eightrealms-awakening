{% if mainQuest %}
    <div class="main-quest">
        <h2 class="quest-type">Quête Principale</h2>
        <div class="content">
            <h3 class="quest-name">{{ mainQuest.quest.name }}</h3>
            {% for playerQuestStep in this.character.playerQuestSteps %}
                {% if playerQuestStep.questStep.quest  == mainQuest.quest and playerQuestStep.status != "skipped" %}
                    {{ playerQuestStep.questStep.description | raw }}
                {% endif %}
            {% endfor %}
        </div>
    </div>
{% endif %}
