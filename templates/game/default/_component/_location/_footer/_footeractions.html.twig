<div class="screen-footeractions">
    {% for npc in this.activeScreen.location.npcs %}
        <a href="#" class="screen-action" title="{{ npc.name }}">
            <img src="{{ asset('img/chapter1/npc/thumb_' ~ npc.picture) }}" alt="{{ npc.name }}"/>
        </a>
    {% endfor %}
</div>
