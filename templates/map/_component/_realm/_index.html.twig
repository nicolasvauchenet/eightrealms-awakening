<div class="map-content realm {% if this.activeContent == 'realm' %}active{% endif %}" data-loading="addClass(loading)">
    <figure class="map-container">
        <img src="{{ asset('img/chapter1/map/' ~ app_mapService.realmMap(this.character).picture) }}" alt="{{ app_mapService.realmMap(this.character).name }}"/>
    </figure>
</div>
